For box model, use MBP = Margin, Border, Padding

Always use 'clear' set to 'both'

Don't abuse '!important'. Use 'class=important' instead

Only set height if necessary

Always use font stack

Use it in order
    a:link
    a:visited
    a:hover
    a:active

In list, 'list-style-position' default value is 'outside'

Use CSS specificity calculator if 

For BG images, adjust the ff. in order:
    background-repeat: no-repeat;
    background-size: contain;
    background-position: center;

Instead of using 'opacity' for color, use 'rgba' instead

When using percentage as size, you can use min/max properties

--

CRL

Always use percentage to maintain responsiveness

Familiarize sizes by thinking of parent-child relationship

